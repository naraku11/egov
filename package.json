{
  "name": "egov-aluguinsan",
  "version": "1.0.0",
  "description": "E-Government Assistance System — Municipality of Aluguinsan, Cebu",
  "type": "module",
  "main": "app.js",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "start": "node app.js",
    "postinstall": "npm --prefix backend install --omit=dev && npm --prefix backend run db:generate",
    "build": "npm --prefix frontend install && npm --prefix frontend run build && npm --prefix backend run db:generate",
    "db:migrate": "npm --prefix backend exec -- npx prisma migrate deploy",
    "db:seed": "npm --prefix backend run db:seed",
    "db:setup": "npm --prefix backend exec -- npx prisma migrate deploy && npm --prefix backend run db:seed",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev"
  }
}
